@model UserRoleUpdateIndexModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>UserRoleUpdateIndexModel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Id" class="control-label"></label>
                <input asp-for="Id" class="form-control" />
                <span asp-validation-for="Id" class="text-danger"></span>
            </div>
            <div>
                <label asp-for="RoleNames" class="control-label"></label>
                <br />
                <select class="form-select-sm" asp-for="SelectedRole" asp-items="@Model.RoleNames"></select>
                <br/>
                <label asp-for="Usernames" class="control-label"></label>
                <br />
                <select class="form-select-sm" asp-for="SelectedUsername" asp-items="@Model.Usernames"></select>
            </div>
            <div class="form-group">               
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
